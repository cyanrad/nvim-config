-- -- color control function
-- function SetGlobalColor(color)
--     color = color or "poimanders"
--     vim.cmd.colorscheme(color)
-- end

-- -- global call to set color on launch
-- SetGlobalColor()
